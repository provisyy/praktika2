const rublesCredit = 200000;
const creditPersent = 0.1 * rublesCredit;
const payPerYear = ((0.5 * rublesCredit) + creditPersent);
const yearCount = 2;
console.log(yearCount * payPerYear);
console.log(creditPersent)